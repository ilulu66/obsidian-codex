{
  "name": "obsidian-codex",
  "version": "0.3.0",
  "private": true,
  "description": "Call Codex from inside Obsidian.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "check": "node --check main.js && node scripts/check-manifest.mjs",
    "package": "node scripts/package.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=18"
  }
}
